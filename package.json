{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/send": "^0.17.4",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "mime": "^4.0.4",
    "send": "^0.18.0"
  }
}